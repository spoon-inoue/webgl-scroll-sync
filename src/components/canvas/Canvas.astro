<webgl-canvas>
  <canvas></canvas>
</webgl-canvas>

<style lang="scss" is:global>
  webgl-canvas {
    --padding-block: 25vh;

    position: absolute;
    z-index: -1;
    top: 0;
    left: 0;
    margin-top: calc(-1 * var(--padding-block));
    width: 100%;
    height: calc(100vh + var(--padding-block) * 2);
    overflow: hidden;

    canvas {
      width: 100%;
      height: 100%;
    }
  }
</style>
